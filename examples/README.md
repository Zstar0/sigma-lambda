# Examples

Examples are intentionally small and generic. They demonstrate the **policy / ops / trace** architecture without leaking domain-specific policy libraries.

- `deployment/` — Example of a “safe deployment” decomposition
